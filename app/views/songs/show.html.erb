<div class="song">
  <h2>Song Release: <%= @song.title %></h2>
  <%= @song.artist_name %>
  <h4>
    <% if @song.release_year %>
      Released: <%= @song.release_year %>
    <% end %>
  </h4>
  <%= link_to 'Edit Song', edit_song_path(song) %>
</div>